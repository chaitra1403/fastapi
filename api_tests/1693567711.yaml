# Test generated by RoostGPT for test demoTest using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          expect:
            - statusCode: 200
            - hasProperty: "content"
            - hasProperty: "content.application/json"
            - hasProperty: "content.application/json.schema"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get:
          url: "/?param=%23%40%24%25%5E%26*"
          expect:
            - statusCode: 200
            - hasProperty: "content"
            - hasProperty: "content.application/json"
            - hasProperty: "content.application/json.schema"
